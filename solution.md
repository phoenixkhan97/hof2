// You must RETURN your result in each function!
// let nums1 = [1,2,3,4]
// const printNums = num1.forEach((nums1) => {
//   return printNums
// })
const nums2 = [1,2,3,4,5]
const returnSums = nums2.forEach((a,b) => {
  for (i = 0; i < numbers.length; i++)
return nums2
})
let objs= {
 n:1,
 n:2,
 n:3}
const returnTotal = objs.forEach((objs)=> {
  let sum = 0
  return sum + objs.n
})
const decimals =[2.30, 2.40, 32.99]
const printMoney = printMoney.map((decimals)=>{
    return "$" + decimals.toFixed(2)
})
const values = {
  name: 'Joe',
  happy: false
}
const returnAllTrueValues = values.filter((values)=> {
  return values.happy === true
})
const nums3 = [1,2,3,4]
const addTwo = nums3.reduce((a,b)=> {
return addTwo(a + b + 2)
})
const strings = ['g', 'f', 'z']
const joinStrings = strings.reduce((a,b)=>{
  return joinStrings(a + b)
})
//Bonus
const objects = {
  value: 1,
  value: 2,
  value: 23
}
const sortObjectsByValue = objects.sort((objects)=> {
  return sortObjectsByValue()
})
